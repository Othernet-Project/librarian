// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var errorMessage, initPlugin, onddForm, section, submitData, url;
  section = $('#dashboard-ondd');
  onddForm = null;
  url = null;
  errorMessage = templates.onddSettingsError;
  submitData = function(data) {
    var res;
    res = $.post(url, data);
    res.done(function(data) {
      onddForm.html(data);
      return ($(window)).trigger('transponder-updated');
    });
    return res.fail(function() {
      return onddForm.prepend(errorMessage);
    });
  };
  initPlugin = function(e) {
    onddForm = $('#ondd-form');
    url = onddForm.attr('action');
    return onddForm.on('submit', function(e) {
      var data;
      e.preventDefault();
      data = onddForm.serialize();
      return submitData(data);
    });
  };
  return section.on('dashboard-plugin-loaded', initPlugin);
})(this, this.jQuery, this.templates);
