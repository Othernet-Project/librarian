// Generated by CoffeeScript 1.10.0
(function(window, $) {
  var CHECK_INTERVAL, statusContainer, statusUrl, updateStatus;
  CHECK_INTERVAL = 3000;
  statusContainer = $('#signal-status');
  if (!statusContainer.length) {
    return;
  }
  statusUrl = statusContainer.data('url');
  updateStatus = function() {
    return statusContainer.load(statusUrl);
  };
  return setInterval(updateStatus, CHECK_INTERVAL);
})(this, this.jQuery);
