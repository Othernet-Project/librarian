// Generated by CoffeeScript 1.10.0
(function(window, $) {
  return $.fn.updownNav = function(selector, skip) {
    var elem;
    elem = $(this);
    if (selector == null) {
      selector = 'a';
    }
    if (skip == null) {
      skip = '.disabled';
    }
    return elem.on('keyup', function(e) {
      elem = $(this);
      switch (e.which) {
        case 38:
          return elem.prevAll(selector + ":not(" + skip + ")").first().focus();
        case 40:
          return elem.nextAll(selector + ":not(" + skip + ")").first().focus();
      }
    });
  };
})(this, this.jQuery);
