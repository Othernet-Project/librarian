// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  'use strict';
  var prepareVideo, videoPlayer;
  videoPlayer = Object.create(mediaPlayer);
  videoPlayer["super"] = mediaPlayer;
  videoPlayer.initialize = function(container) {
    return this["super"].initialize.call(this, container);
  };
  videoPlayer.readyPlayer = function() {
    var controls;
    controls = this.container.find('#video-controls-video').first();
    controls.mediaelementplayer({
      features: ['playpause', 'progress', 'duration', 'volume', 'fullscreen'],
      success: (function(_this) {
        return function(mediaElement) {
          _this.onPlayerReady(mediaElement);
        };
      })(this)
    });
  };
  videoPlayer.updatePlayer = function(item) {
    this["super"].updatePlayer.call(this, item);
    this.player.play();
  };
  prepareVideo = function() {
    var controls;
    controls = $('#video-controls');
    if (!controls.length) {
      return;
    }
    videoPlayer.initialize($('#main-container'));
  };
  prepareVideo();
  window.onTabChange(prepareVideo);
})(this, this.jQuery, this.templates);
