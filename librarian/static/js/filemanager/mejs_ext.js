// Generated by CoffeeScript 1.10.0
(function(window, $) {
  'use strict';
  $.extend(mejs.MepDefaults, {
    nextText: 'Next',
    prevText: 'Previous'
  });
  $.extend(MediaElementPlayer.prototype, {
    buildnext: function(player, controls, layers, media) {
      var nextButton, nextButtonHandler, nextButtonTemplate;
      nextButtonTemplate = '<div class="mejs-button mejs-next-button mejs-next">' + '<button type="button" aria-controls="' + player.id + '" title="' + player.options.nextText + '"></button>' + '</div>';
      nextButtonHandler = function(e) {
        $(media).trigger('mep-ext-playnext');
      };
      nextButton = $(nextButtonTemplate).appendTo(controls).click(nextButtonHandler);
    },
    buildprev: function(player, controls, layers, media) {
      var prevButton, prevButtonHandler, prevButtonTemplate;
      prevButtonTemplate = '<div class="mejs-button mejs-prev-button mejs-prev">' + '<button type="button" aria-controls="' + player.id + '" title="' + player.options.prevText + '"></button>' + '</div>';
      prevButtonHandler = function(e) {
        $(media).trigger('mep-ext-playprev');
      };
      prevButton = $(prevButtonTemplate).appendTo(controls).click(prevButtonHandler);
    }
  });
})(this, this.mejs.$);
