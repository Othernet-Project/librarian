// Generated by CoffeeScript 1.10.0
(function(window, $, templates) {
  var activateTab, firstTab, setupTabs;
  setupTabs = function() {
    var sidebar, tabControls;
    tabControls = $(templates.playlistTabs);
    sidebar = $('#views-sidebar');
    sidebar.addClass('with-tabs');
    sidebar.prepend(tabControls);
    tabControls.on('click', 'a', function(e) {
      e.preventDefault();
      return activateTab(this);
    });
    return (tabControls.find('a')).first();
  };
  activateTab = function(button) {
    var href;
    ($('.playlist-section-open')).removeClass('playlist-section-open');
    ($('.playlist-tabs .active')).removeClass('active');
    button = $(button);
    button.addClass('active');
    href = button.attr('href');
    return ($(href)).addClass('playlist-section-open');
  };
  if (templates.playlistTabs != null) {
    firstTab = setupTabs();
    return activateTab(firstTab);
  }
})(this, this.jQuery, this.templates);
