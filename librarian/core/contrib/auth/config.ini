[exports]
database = librarian
database_sets = auth
migrations = migrations

commands =
    commands.NoAuthentication
    commands.CreateSuperuserCommand

hooks =
    hooks.initialize

plugins =
    plugins.user_plugin
